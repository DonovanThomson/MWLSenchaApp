/**
 * The model used by {@link Examples.store.Kittens}.
 */
Ext.define('VulaMobi.model.FeedbackSite', {
    extend: 'Ext.data.Model',

    config: {
        fields: [
            "name", "siteid"

        ]
    }
});
